#pragma once

enum vague_token_t {
	WORD,
	NUM,
	SYM,
};

enum token_t {
	UNKNOWN,

	INSTRUCTION,
	INSTRUCTION_END,
	IDENTIFIER,

	LOCAL,
	GLOBAL,
	LABEL_BEG,
	ARG_SEPARATOR,

	ADD_OP,
	SUB_OP,

	HEXADECIMAL_LITERAL,
	INTEGER_LITERAL,
	CHAR_LITERAL,
	STRING_LITERAL,

	INVALID,
};
